{
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "npm --prefix frontend run build",
    "test:simulation": "npm --prefix frontend run test:simulation"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1"
  }
}
